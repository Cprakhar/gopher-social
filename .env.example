# Gopher Social - Example Environment Variables
# Copy this file to `.env` and adjust values as needed.

########################################
# Core Application
########################################
# Server listen address
ADDR=:8080
# External API base URL (public URL the API is reachable at)
EXTERNAL_URL=http://localhost:8080
# Web client base URL
WEB_URL=http://localhost:3000
# Environment (development|staging|production)
ENV=development
# Application version string
VERSION=0.0.1

########################################
# Database
########################################
# Postgres connection string
DB_ADDR=postgres://admin:adminpassword@localhost/gopher-social?sslmode=disable
# Connection pool sizing
DB_MAX_CONNS=30
DB_MIN_CONNS=10
# Connection max idle time (Go duration)
DB_MAX_IDLE_TIME=15m
# Connection max lifetime (Go duration)
DB_MAX_CONN_LIFETIME=1h

########################################
# Mail / Invitations
########################################
# Invitation token expiration (Go duration)
MAIL_EXP=72h
# Email API provider key (leave blank if disabled)
MAIL_API_KEY=
# Mail sender address
MAIL_SENDER=

########################################
# Authentication
########################################
# Basic auth credentials (for protected admin endpoints, if any)
BASIC_AUTH_USERNAME=admin
BASIC_AUTH_PASSWORD=adminpassword
# Token (JWT) secret — CHANGE IN PRODUCTION
AUTH_TOKEN=change-me-super-secret
# Token expiration (Go duration)
AUTH_TOKEN_EXP=72h
# Token issuer and audience
AUTH_TOKEN_ISS=gopher-social
AUTH_TOKEN_AUD=gopher-social

########################################
# Redis (optional)
########################################
REDIS_ADDR=localhost:6379
REDIS_PASSWORD=
REDIS_DB=0
REDIS_ENABLED=true

########################################
# Rate Limiter
########################################
# Max requests within the timeframe
RATELIMITER_REQUESTS_COUNT=20
# Enable / disable rate limiter
RATELIMITER_ENABLED=true

########################################
# Notes
# - Durations use Go format, e.g. 15m, 1h, 72h.
# - Leave MAIL / Redis / Auth values blank or disabled if not used.
# - Ensure AUTH_TOKEN is set to a strong secret in any non-local environment.
